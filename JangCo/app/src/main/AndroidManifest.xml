<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jangco">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ChangeSQualificationActivity"></activity>
        <activity android:name=".ChangeIncomeActivity" />
        <activity android:name=".ChangeGradeActivity" />
        <activity android:name=".ChangeSchoolActivity" />
        <activity android:name=".ChangeAddressActivity" />
        <activity android:name=".ChangeProfileActivity" />
        <activity android:name=".ChangePwActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".GuideActivity" />
        <activity android:name=".EventBoardActivity" />
        <activity android:name=".NoticeBoardActivity" />
        <activity android:name=".SearchAddressActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.jangco.file_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>
    </application>

</manifest>